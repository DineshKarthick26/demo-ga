name: Hello World
on: push
jobs:
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
      branches:
        - main
  build:
    runs-on: ubuntu-slim
    steps:
      - name: build step
        run: echo "hi, welcome"

  

        

