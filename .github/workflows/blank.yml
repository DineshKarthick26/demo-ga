name: Hello World
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - name: build step
        run: echo "hi, welcome"
  test:
      runs-on: ubuntu-slim
      needs: build
      steps:
        - name: test-phase
          run: echo "It is in a testing stage"
  deploy:
       runs-on: ubuntu-slim
       needs: test
       steps: 
         - name: deploy-stage
           run: echo "Please wait till deployment"
    
  

        

