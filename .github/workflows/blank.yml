name: Action demo
on: push
jobs:
  action-demo:
    runs-on: ubuntu-slim
    steps:
      - name: checkout the code
        uses: actions/checkout@v4
      - name: print index.html file
        run: cat index.html

  

        

