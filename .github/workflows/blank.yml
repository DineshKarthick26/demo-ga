name: Hello World
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
      branches:
        - main
jobs:
  build:
    runs-on: ubuntu-slim
    steps:
      - name: build step
        run: echo "hi, welcome"

  

        

